apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: microservice-a
  namespace: apps
spec:
  replicas: 2
  strategy:
    blueGreen:
      activeService: service-a
      previewService: service-a-preview
  selector:
    matchLabels:
      app: microservice-a
      app.kubernetes.io/name: microservice-a
      app.kubernetes.io/part-of: microservices
  template:
    metadata:
      labels:
        app: microservice-a
        app.kubernetes.io/name: microservice-a
        app.kubernetes.io/part-of: microservices
    spec:
      containers:
        - name: nginx
          image: nginx:alpine-01
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: index-html
              mountPath: /usr/share/nginx/html/index.html
              subPath: index.html
          resources:
            requests:
              cpu: 50m
              memory: 32Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        - name: index-html
          configMap:
            name: microservice-a-html
            items:
              - key: index.html
                path: index.html
