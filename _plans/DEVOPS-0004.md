<!--
  SPEC-DRIVEN DEVELOPMENT — HOW TO USE THIS FILE
  ================================================
  This file is AUTO-GENERATED by Copilot from _specs/DEVOPS-0004.md.
  Do NOT create or edit this file manually before reviewing the spec.

  Engineer review checklist before approving:
  - [ ] All files to create/modify are listed and make sense
  - [ ] Implementation steps are in the correct order
  - [ ] Validation steps match the acceptance criteria in the spec
  - [ ] No files are missing from kustomization.yaml updates
  - [ ] Branch name matches the task number

  Once approved, tell Copilot: "implement" — it will follow this plan exactly.
  If you want changes, edit this file first, then say "implement".
-->

# Plan: DEVOPS-0004

## Branch
`DEVOPS-0004`

## Summary
Scale the microservice-b Argo Rollout from 2 to 4 replicas to increase throughput capacity.

## Files to Create
| File | Purpose |
|------|---------|
| _(none)_ | No new files required |

## Files to Modify
| File | Change |
|------|--------|
| `applications/microservices/microservice-b/deployment.yaml` | Change `spec.replicas` from `2` to `4` |
| `CHANGELOG.md` | Add entry for replica scale-up under next version section |

## Implementation Steps
1. In `applications/microservices/microservice-b/deployment.yaml`, change `replicas: 2` to `replicas: 4`.
2. In `CHANGELOG.md`, add a new `## [Unreleased]` (or patch version) entry noting the replica increase for microservice-b.
3. Run the `validate-manifests` skill to confirm `kubectl kustomize applications/` exits 0.
4. Stage all changed files for engineer review.

## Validation
- [ ] `spec.replicas: 4` is present in `applications/microservices/microservice-b/deployment.yaml`
- [ ] `kubectl kustomize applications/` exits 0 with no errors
- [ ] CHANGELOG.md contains an entry for the microservice-b replica increase
- [ ] All changes are on branch `DEVOPS-0004`

## Risks / Decisions
- **No HPA present**: replicas are set statically; the new value of 4 will be respected by Argo Rollouts directly.
- **Blue-green strategy**: increasing replicas also scales the preview ReplicaSet during a rollout; no functional impact when no active rollout is in progress.
- **CHANGELOG version**: latest version is `[3.2.0]`; this is a non-breaking operational change — add as a new `## [Unreleased]` section that will become a patch bump on merge.
