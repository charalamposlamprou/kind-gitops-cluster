apiVersion: v1
kind: Service
metadata:
  name: service-b
  namespace: apps
spec:
  type: ClusterIP
  selector:
    app: microservice-b
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
