<!--
  SPEC-DRIVEN DEVELOPMENT — HOW TO USE THIS FILE
  ================================================
  This file is AUTO-GENERATED by Copilot from _specs/DEVOPS-0005.md.
  Do NOT create or edit this file manually before reviewing the spec.

  Engineer review checklist before approving:
  - [ ] All files to create/modify are listed and make sense
  - [ ] Implementation steps are in the correct order
  - [ ] Validation steps match the acceptance criteria in the spec
  - [ ] No files are missing from kustomization.yaml updates
  - [ ] Branch name matches the task number

  Once approved, tell Copilot: "implement" — it will follow this plan exactly.
  If you want changes, edit this file first, then say "implement".
-->

# Plan: DEVOPS-0005

## Branch
`DEVOPS-0005`

## Summary
Scale the microservice-b Argo Rollout replicas down from 4 to 2.

## Files to Create
| File | Purpose |
|------|---------|
| _(none)_ | No new files required |

## Files to Modify
| File | Change |
|------|--------|
| `applications/microservices/microservice-b/deployment.yaml` | Change `spec.replicas` from `4` to `2` |
| `CHANGELOG.md` | Add entry for replica scale-down under `[Unreleased]` section |

## Implementation Steps
1. In `applications/microservices/microservice-b/deployment.yaml`, change `replicas: 4` to `replicas: 2`.
2. In `CHANGELOG.md`, add a line under the existing `## [Unreleased]` section noting the replica scale-down for microservice-b.
3. Run the `validate-manifests` skill to confirm `kubectl kustomize applications/` exits 0.
4. Stage all changed files for engineer review.

## Validation
- [ ] `spec.replicas: 2` is present in `applications/microservices/microservice-b/deployment.yaml`
- [ ] `kubectl kustomize applications/` exits 0 with no errors
- [ ] CHANGELOG.md contains an entry for the microservice-b replica scale-down
- [ ] All changes are on branch `DEVOPS-0005`

## Risks / Decisions
- **Revert of DEVOPS-0004**: this directly undoes the scale-up committed in DEVOPS-0004; no other dependencies affected.
- **Blue-green strategy**: reducing replicas during an active rollout would affect both active and preview ReplicaSets; safe when no rollout is in progress.
- **CHANGELOG**: an `[Unreleased]` section already exists from DEVOPS-0004; add the new entry there rather than opening a second unreleased block.
